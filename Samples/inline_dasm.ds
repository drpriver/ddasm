import io
let x = 0
function start(){
    label s
    dasm '
        call function foo
        read rs0 var x
        cmp rs0 10
        cmov ne rip label Ls
    '
}

function foo(){
    x = x + 1
    io.printf("hello from foo, x = %d\n", x)
}
